import { dev } from '$app/environment';

export const title = 'NetroWorks';
export const description = "Netro's landing page and documentation.";
export const url = dev ? 'http://localhost:5173' : 'https://netro.works';
export const ogImage = `${url}/og-image.png`;
